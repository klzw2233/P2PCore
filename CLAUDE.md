# CLAUDE.md

Repo-level guidance for P2PCore. Refines the global `~/.claude/CLAUDE.md`.

## Agent skills

### Issue tracker

GitHub Issues via the `gh` CLI (no remote configured yet). See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, used verbatim as label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
