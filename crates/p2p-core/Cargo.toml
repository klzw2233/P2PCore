[package]
name = "p2p-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Session API over the trust layer. Wiring to iroh is a later spec (issue #2)."

[lints]
workspace = true

[dependencies]
p2p-trust = { path = "../p2p-trust" }
